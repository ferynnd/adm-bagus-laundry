<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_dialog"
    android:padding="20dp">

    <!-- Icon Warning -->
    <ImageView
        android:id="@+id/iconWarning"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:src="@drawable/ic_warning"
        app:tint="@color/red"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginBottom="10dp"/>

    <TextView
        android:id="@+id/titleDialog"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Hapus Transaksi"
        android:textSize="20sp"
        android:textColor="@color/blue900"
        android:fontFamily="@font/inter_bold"
        android:textAlignment="center"
        app:layout_constraintTop_toBottomOf="@id/iconWarning"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="10dp"/>

    <TextView
        android:id="@+id/messageDialog"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Pilih jenis penghapusan yang diinginkan:"
        android:textSize="14sp"
        android:textColor="@color/gray900"
        android:fontFamily="@font/inter"
        android:textAlignment="center"
        app:layout_constraintTop_toBottomOf="@id/titleDialog"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="10dp"/>

    <!-- Transaction Info -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cardInfo"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:cardBackgroundColor="@color/blueBase"
        app:cardCornerRadius="10dp"
        app:cardElevation="0dp"
        app:layout_constraintTop_toBottomOf="@id/messageDialog"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="15dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="15dp">

            <TextView
                android:id="@+id/textTransactionNumber"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="No. Transaksi: CAB001234"
                android:textSize="14sp"
                android:textColor="@color/blue900"
                android:fontFamily="@font/inter_semibold"/>

            <TextView
                android:id="@+id/textRecipient"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="Penerima: NANDOO"
                android:textSize="12sp"
                android:textColor="@color/gray900"
                android:fontFamily="@font/inter"
                android:layout_marginTop="5dp"/>

        </LinearLayout>

    </androidx.cardview.widget.CardView>

    <!-- Warning Text -->
    <TextView
        android:id="@+id/warningText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="⚠️ Hapus Permanen tidak dapat dibatalkan!"
        android:textSize="12sp"
        android:textColor="@color/red"
        android:fontFamily="@font/inter_medium"
        android:textAlignment="center"
        app:layout_constraintTop_toBottomOf="@id/cardInfo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="15dp"/>

    <!-- Buttons -->
    <LinearLayout
        android:id="@+id/buttonContainer"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:layout_constraintTop_toBottomOf="@id/warningText"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="20dp">

        <!-- Soft Delete Button -->
        <Button
            android:id="@+id/buttonSoftDelete"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Hapus Sementara"
            android:textColor="@color/white"
            android:backgroundTint="@color/orange_dialog"
            android:fontFamily="@font/inter_semibold"
            android:drawableLeft="@drawable/ic_archive"
            android:drawableTint="@color/white"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"/>

        <!-- Permanent Delete Button -->
        <Button
            android:id="@+id/buttonPermanentDelete"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Hapus Permanen"
            android:textColor="@color/white"
            android:backgroundTint="@color/red_dialog"
            android:fontFamily="@font/inter_semibold"
            android:drawableLeft="@drawable/ic_delete_forever"
            android:drawableTint="@color/white"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:layout_marginTop="10dp"/>

        <!-- Cancel Button -->
        <Button
            android:id="@+id/buttonCancel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Batal"
            android:textColor="@color/blue900"
            android:backgroundTint="@color/blueBase"
            android:fontFamily="@font/inter_semibold"
            android:layout_marginTop="10dp"/>

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>